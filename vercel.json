{
    "version": 2,
    "builds": [
        {
            "src": "src/shared/server.ts",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "src/shared/server.ts"
        }
    ]
}